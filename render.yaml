services:
  - type: worker
    name: polymarket-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python scanner.py
    envVars:
      - key: TELEGRAM_TOKEN
        sync: false
      - key: TELEGRAM_CHAT_ID
        sync: false
